<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title><%= title %></title>

    <style>
        :root{
            --bg:#f6f8fb;
            --card:#ffffff;
            --accent:#2b6cb0;
            --muted:#6b7280;
            --border: #e6e9ef;
            --radius:10px;
            --max-width:1100px;
        }
        *{box-sizing:border-box}
        body{
            margin:0;
            font-family:Inter, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
            background:var(--bg);
            color:#0f172a;
            -webkit-font-smoothing:antialiased;
            -moz-osx-font-smoothing:grayscale;
            padding:24px;
            display:flex;
            justify-content:center;
        }

        .page{
            width:100%;
            max-width:var(--max-width);
        }

        header{
            display:flex;
            align-items:center;
            justify-content:space-between;
            margin-bottom:20px;
        }
        header h1{
            margin:0;
            font-size:1.25rem;
            color:var(--accent);
        }
        header .meta{
            font-size:0.95rem;
            color:var(--muted);
        }

        .products{
            list-style:none;
            padding:0;
            margin:0;
            display:grid;
            grid-template-columns:repeat(auto-fill,minmax(260px,1fr));
            gap:18px;
        }

        .product-card{
            background:var(--card);
            border:1px solid var(--border);
            border-radius:var(--radius);
            padding:16px;
            box-shadow:0 6px 20px rgba(12,20,41,0.04);
            display:flex;
            flex-direction:column;
            gap:8px;
            transition:transform .12s ease, box-shadow .12s ease;
        }
        .product-card:hover{
            transform:translateY(-4px);
            box-shadow:0 12px 30px rgba(12,20,41,0.08);
        }

        .product-card h2{
            margin:0;
            font-size:1rem;
            color:#0b2545;
        }

        .product-meta{
            display:flex;
            gap:12px;
            align-items:center;
            font-size:.95rem;
            color:var(--muted);
        }

        .price{
            margin-left:auto;
            font-weight:600;
            color:#0b3c66;
            background:linear-gradient(90deg,#e6f2ff,#fff);
            padding:4px 8px;
            border-radius:6px;
            border:1px solid #e1efff;
        }

        .description{
            margin:6px 0 0 0;
            font-size:.95rem;
            color:#263241;
            line-height:1.3;
        }

        @media (max-width:500px){
            body{padding:12px;}
            .product-card{padding:12px}
        }
    </style>
</head>
<body>
    <div class="page">
        <header>
            <h1><%= title %></h1>
            <div class="meta">Welcome, <strong><%= userName %></strong></div>
        </header>

        <% if (!items || items.length === 0) { %>
            <p class="muted">No products available.</p>
        <% } else { %>
            <ul class="products">
                <% items.forEach(function(item){ %>
                    <li class="product-card">
                        <h2><%= item.productName %></h2>

                        <div class="product-meta">
                            <span>Quantity: <strong><%= item.quantity %></strong></span>
                            <span class="price">$<%= item.price %></span>
                        </div>

                        <p class="description"><%= item.description %></p>
                    </li>
                <% }); %>
            </ul>
        <% } %>
    </div>
</body>
</html>